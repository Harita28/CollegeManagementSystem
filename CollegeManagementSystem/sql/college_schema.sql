-- Create database
CREATE DATABASE IF NOT EXISTS college_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE college_db;

-- Identity tables will be created by EF migrations; but provide simplified tables if you wish to import directly:
-- For easier start, you can rely on EF Core migrations (recommended).
